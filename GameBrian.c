#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include "game.h"

/* POSSIBLE STRUCTS */
// Struct for University Resources (to hold data about university resources i.e discplines + publications + IPs)

// returns the content of the given vertex (campus code or VACANT_VERTEX)
void getCampus (Game g, vertex v) {
	int vertexContent = 0;
}